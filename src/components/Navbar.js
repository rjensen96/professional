import React from 'react';
import '../App.css';

export const Navbar = () => {
    return (
        <header className="mainHeader">
            <h1 className="title">.ryanjensen()</h1>
        </header>
    )
}